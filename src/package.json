{
	"name": "browsers-storage-options-demo",
	"browserslist": "default, not dead",
	"source": "index.html",
	"module": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"start": "parcel",
		"build": "parcel build",
		"lint": "eslint",
		"check": "tsc --noEmit",
		"ci": "pnpm build && pnpm lint && pnpm check && pnpm test"
	},
	"devDependencies": {
		"@parcel/config-default": "^2.6.0",
		"@parcel/core": "^2.6.0",
		"@parcel/transformer-sass": "^2.6.0",
		"@parcel/transformer-typescript-tsc": "^2.6.0",
		"parcel": "^2.6.0",
		"typescript": "^4.7.2"
	},
	"dependencies": {
		"eslint": "^8.16.0"
	}
}
